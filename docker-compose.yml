
services:
  web:
    image: paulomg96/auto-api-treinamento:1.0.0
    command: uvicorn server.asgi:application --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app
    ports:
      - "8004:8000"